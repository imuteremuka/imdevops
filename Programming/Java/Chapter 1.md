# JavaScript

Javascript is a programming language mainly used for the the frontend but it is now used for the backend as well

## What can it do

It can be used to build full blown apps

## Where does it run

It runs on all browsers and now is embebbed with C++ and Java to produce node

# Setting up Java Script

- a) VS Code
- b) Install node
- C) the extension is .js
- d) Install Live Server extension
- e) to run a Javascript program type node <programname>

# Hello World

    - !+space to generate an html template

# Seperation of concerns

    - Separate .js and .html code as best practice
    - Each line is separated by ;

# Variables

- it used to store values temporary
- can not be a reserved word
- must be meaningful
- can not start with a number
- can not contain a space
- try to use Camel Notation as best practice
- They are Case sensitive
- Initliase them as best practive
- The let statement is used to declare variables

# Constants

- They dont change value
- the start with a default word const <constantName>

# Primitive Datatypes (Primitive Data types)

- string =in between qoutes
- boolean = true or false
- numbers = float, undefined, null,

# Objectuve

is used to group related varibles together

# Function

- is used to perform a particuler task
- they normal receive a value
